class FastScreenRecorderWidget extends StatelessWidget {
  final Widget child;

  const FastScreenRecorderWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO
    return child;
  }
}
